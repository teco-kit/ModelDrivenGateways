import Automata;
extension edu::teco::automata::generator::templates::util;

int bitsLen(TTimestamp s): 32;

int bitsLen(IntType s):   bitsLen(s.min,s.max,s.stepping);
boolean signed(IntType s): (s.min<0);
